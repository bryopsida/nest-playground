apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nest-playground.fullname" . }}-test-connection"
  labels:
    {{- include "nest-playground.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nest-playground.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
